# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DATABASE_URL=postgresql://carsync_user:carsync_password@postgres:5432/carsync_expenses_db

# Optional: Local PostgreSQL (if not using Docker)
# DATABASE_URL=postgresql://carsync_user:carsync_password@localhost:5432/carsync_expenses_db

# Authentication
AUTH_SECRET=change-me-in-production
